{
  "name": "odds-platform-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Portfolio-ready odds platform: TypeScript API, Next.js frontend, and Go edge service",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @portfolio/shared && npm run build -w @portfolio/api && npm run build -w @portfolio/web",
    "dev": "npm run dev -w @portfolio/api",
    "test": "npm run test -w @portfolio/api",
    "smoke": "bash ./scripts/smoke.sh local",
    "smoke:docker": "bash ./scripts/smoke.sh docker"
  }
}
